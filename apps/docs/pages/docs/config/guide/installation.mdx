# Installation

Let's get started with `@neato/config`. First lets install the package.

```sh npm2yarn
npm i @neato/config
```

<Note>
Thats it! Next up: [find out how to use the library](./usage.mdx).
</Note>
